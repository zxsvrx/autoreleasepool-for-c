- Add alternative memory management (realloc with more elements?)
- Add different default allocation modes
- Use error logs instead of printf
- Add own destructors (alloc_c(ownStruct, func(ownStruct), n))
- Add reference counting/passing (ownership transfer)
